

export const initialOurStoryState = {
    ourStoryStateTest: "Our story State is work!",
    loading: false,
    loaded: false
};

export const ourStoryReducer = (state, action) => {

    return {...state};
};