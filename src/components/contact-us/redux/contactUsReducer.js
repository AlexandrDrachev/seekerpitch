

export const initialContactUsState = {
    contactUsStateTest: "Contact Us State is work!",
    loading: false,
    loaded: false
};

export const contactUsReducer = (state, action) => {

    return {...state};
};